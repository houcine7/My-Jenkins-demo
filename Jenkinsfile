pipeline{

    agent any

    stages{
        
        stage("build") {
           steps{
             echo 'BUILDING TEH APP'
           }
        }

        stage("test") {
           steps{
             echo 'TESTING TEH APP'
           }
        }

        stage("deploy") {
            steps{
                echo 'DEPLOYING TEH APP'
            }
        }

    }
}