pipeline{

    agent any

    stages{
        
        stage("build") {
            echo "BUILDING TEH APP"
        }

        stage("test") {
            echo "TESTING TEH APP"
        }

        stage("deploy") {
            echo "DEPLOYING TEH APP"
        }

    }
}